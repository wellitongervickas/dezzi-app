export const mobile = '360px';
export const tablet = '768px';
export const monitor = '1024px';
export const monitorWide = '1280px';

export const xs = (mult = 1) => `${8 * mult}px`;
export const sm = (mult = 1) => `${16 * mult}px`;
export const md = (mult = 1) => `${32 * mult}px`;
export const lg = (mult = 1) => `${64 * mult}px`;
export const xl = (mult = 1) => `${128 * mult}px`;
